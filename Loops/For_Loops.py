# This loop iterate over a sequense 

numbers = [10, 15, 20, 25]
# list of numbers
for number in numbers:
    # the for keyword iterate over numbers and assign each value to the control variable number
    print(number)
